#!/usr/bin/env bash
# Rally entry point. Keeps the python layout an implementation detail.
set -euo pipefail
ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec python3 "$ROOT/src/runner.py" "$@"
